# 指定服务端口
server:
  port: 8899

# 配置连接数据库的参数
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/work_test1?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root

# 日志显示级别
logging:
  level:
    com.example.worktext1: trace
